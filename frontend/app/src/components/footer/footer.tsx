import { Typography } from '@mui/material';
import { Box } from '@mui/material';

export function Footer(){
    return (
    <Box style={{ backgroundColor: '#f5f5f5', padding: '20px', marginTop: '20px' }}>
        <Typography align="center">Contact information</Typography>
    </Box>
    )
}